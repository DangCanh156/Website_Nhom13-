<!DOCTYPE html>
<html lang="en">
  <head>
    <title>ChonGhe</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>

    button {
        background-color: #4169e1;
        color: white;
        padding: 14px 20px;
        margin: 8px 0;
        border: none;
        cursor: pointer;
        width: 10.5%;
        border-radius: 3em;

    }
    </style>
    </head>


    
<body>
    <div id="container"></div>
    <body>
        <div ng-controller="MyCtrl">
        <hr style="height:7px;color: black;width: 30%; margin: 10 auto;text-align: center">
        <p style="width: 50%; margin: 0 auto;text-align: center">Màn hình</p>
    </div>
    
    

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "A" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
            container.style.position = "absolute";
        container.style.left = "50%";
        container.style.top = "50%";
        container.style.transform = "translate(-50%, -50%)";
            
        }
    </script>
    
    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "B" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "C" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "D" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "E" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "F" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "G" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "H" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "J" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
        }
    </script>

    <script>
        var container = document.getElementById("container");
        for (var i = 0; i < 9; i++) {
            var button = document.createElement("button");
            button.textContent = "K" + (i + 1);
            container.appendChild(button);
            button.onclick = function() {
                this.style.backgroundColor = "red";
            };
            
        }
    </script>
    
    
   </body>
